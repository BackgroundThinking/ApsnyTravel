# --- CLIENT SIDE (Public) ---
# The URL of the serverless function.
# For local dev: http://localhost:3000/api/book
# For production: Leave empty to use relative path '/api/book' or specify full URL
# Access via: import.meta.env.VITE_BOOKING_ENDPOINT
VITE_BOOKING_ENDPOINT=http://localhost:3000/api/book

# Set to 'true' to bypass the backend and simulate success (for UI testing)
# Access via: import.meta.env.VITE_USE_MOCK
VITE_USE_MOCK=false

# --- SERVER SIDE (Private / Vercel Only) ---
# These are NOT visible to the browser. Access via: process.env.VARIABLE_NAME
# Get this from @BotFather
TELEGRAM_BOT_TOKEN=
# The ID of the Admin Group (e.g. -1003392184853)
TELEGRAM_CHAT_ID=
